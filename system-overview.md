# 萊廷照明 內部業務查詢系統 — 系統設計說明

> 對應版本：篩選器原型 v0.5（2026-05-29）
> 線上 Demo：<https://leonardo101101078-beep.github.io/laitin-search-v2/>
> 適用對象：決策者 / 代理商與業務 / 內部開發 — 一份通用說明

本文件用產品經理的角度，說明三件事：**(一)搜索怎麼設計、怎麼對應原始資料；(二)卡片與詳情頁怎麼設計；(三)目前 Demo 後續如何接上完整 PRD 開發。**

---

## 一、搜索邏輯如何設計？如何對應原始資料

核心目標：讓業務員在客戶現場 **30 秒內**完成產品查詢，取代「翻紙本型錄 + 打電話回公司」。
做法是把找東西拆成**兩種互補的方式**，且兩者**同時生效、取交集**（一邊打字、一邊用條件縮小）。

### 1.1 資料基礎 — Excel 三色分層

系統的單一事實來源是 `萊廷照明-產品資料庫.xlsx`。欄位依角色分成三種顏色：

| 顏色 | 角色 | 範例欄位 | 系統如何使用 |
|------|------|---------|-------------|
| 🔵 **藍（共通核心）** | 三系列都有的欄位 | 系列、主標、副標、簡述、主型號、標籤、零售價 | 搜索與卡片的主幹 |
| 🩷 **粉（系列獨有）** | 每系列各自不同的規格 | 鋁線槽：底/高/燈槽寬；燈帶：電壓/功率/防水等級 | 動態生成專屬篩選器 |
| 🔴 **紅（內部定價）** | 牌價/批發/工程/優惠/單位 | — | **本期完全不進系統**，未來依帳號層級才顯示 |

> **設計原則：Excel 是唯一資料來源。** 網頁的搜索條件與卡片欄位全部由資料「自動長出來」，不是寫死的——日後 Excel 新增系列或欄位，系統能自動對應。

目前三個系列、抽樣 53 筆代表性產品：

- **A. 鋁線槽系列**（15 筆）
- **D. 燈帶控制器系列**（14 筆）
- **N1. 輕智能系列**（24 筆）

> 🔍 **搜索入口（v0.5）**：點右上角放大鏡，即可隨時叫出搜索面板 —— 內含「一般搜索」與「進階搜索」，不必捲回頁面頂端。整頁只有這一個搜索入口，操作一致。

### 1.2 第一種找法 — 一般搜索（打字即時找）

- 輸入後 **≤100ms 即時更新**，不必按搜索鍵。
- 比對範圍是一份「白名單」：**主型號、品項名稱、型號、簡述、標籤、副標**。
- 支援**數值範圍搜索**：例如輸入「70」，會自動命中規格寫「50~90」的產品（落在區間內即命中）。

### 1.3 第二種找法 — 進階搜索（兩段式條件篩選）

```
① 先選「系列」（鋁線槽 / 燈帶控制器 / 輕智能）── 必選
② 共通篩選：主標 → 副標 → 零售價區間（＋全部系列時的「全部標籤」）
③ 系列獨有屬性（選了系列才展開）：該系列「標籤」＋ 依該系列規格自動生成的欄位
```

關鍵設計決定：

- **標籤跟著系列走**（v0.5 依回饋優化）：選了具體系列，標籤下拉移到「系列獨有屬性」最上方，只列該系列標籤；「全部系列」時則在共通區顯示全部標籤 —— 不會一次冒出 200 多種無關標籤。
- **同欄位選多項 = 全部都要符合（AND）**：越選越精準。
- **動態顯示規則**：某規格欄位若整個系列只有一種值（或全空），就自動隱藏，畫面不囉嗦。
- **下拉皆筆畫排序**：數字 → 中文（筆畫少到多）→ 英文 A–Z，方便目視尋找。

### 1.4 結果如何決定

```
最終顯示 = 一般搜索結果 ∩ 進階搜索結果
```

- 沒選的條件 = 不約束（含空值產品都通過）。
- 選了具體值 = 該產品必須符合，**空值產品排除**。

> **重要：搜索完全在使用者的瀏覽器內運算，不呼叫任何 AI、不耗任何運算費用。** 用幾次都不花錢。

---

## 二、卡片頁面如何設計（縮圖 + 詳情）

分成「列表縮圖卡」與「點開後的詳情 Modal」兩層，v0.4 依回饋大幅優化。

### 2.1 列表縮圖卡（一眼掃過）

- **圖片固定 4:3 橫向置中**，不裁切變形；空白處淺灰補滿，整排對齊好閱讀。
- 資訊層級精簡：
  - **主標**（大字）
  - **副標（藍底標籤）＋ 型號** 同一行
  - 簡述 → 標籤
- **庫存（左）與價格（右）置於卡片底部**：業務一眼看到「有沒有貨、多少錢」。
- 移除內部編號等列表不需要的雜訊。

### 2.2 詳情 Modal（點卡片彈出，背景列表不跑掉）

- **上方**：主標 + 副標 + 簡述 + 庫存，先看懂這是什麼。
- **中間**：產品大圖；往下滑時**圖片黏在頂部**持續可見。
- **下方規格表**：
  - 從「品項名稱」開始；**空白欄位自動不顯示**。
  - **隔行深淺底色**提升可讀性。
  - **標籤排在最後**，沿用單色標籤樣式。
- **左右切換相鄰產品**：手機滑動、桌面箭頭 / 鍵盤 ← →，比價快速。
- **價格區預設遮蔽**，點擊才顯示；關閉再開會自動藏回——保護報價隱私。

> ⚠️ 目前 Demo 的多層價格為「**示意用假價**」（由零售價推算 5 個層級）。真實底價待後端串接後，依登入帳號層級顯示。

---

## 三、目前是 Demo，後續如何接上完整 PRD 開發

目前版本定位是 **「可驗證的互動原型」**：讓決策者與業務先把**體驗與規則**確認到位，避免後端做下去才發現方向錯誤。

### 3.1 現在 Demo 的樣子（刻意從簡）

- 純前端網頁 + 資料直接內嵌（**無後端、無資料庫、零運算成本**）。
- 價格是假的、無登入、無跨裝置同步。
- 好處：改得快、隨時上線給業務試、零成本驗證。

### 3.2 銜接正式系統的路線（對齊 PRD）

| 階段 | 要補上的能力 | 對應 PRD 功能 |
|------|------------|--------------|
| **A. 後端地基** | Supabase 資料庫 + 登入 + 三層帳號權限 | F-01 / F-02 / F-03 |
| **B. 真實定價** | 底價由**後端依帳號層級返回**（內部 / 一級 / 二級代理商），絕不在前端過濾 | 定價安全 |
| **C. 報價流程** | 報價清單加入 / 數量 / 小計，**跨裝置同步** | F-06 |
| **D. 管理後台** | 管理員建立帳號、**Excel 批量匯入產品** | F-07 / F-08 |
| **E. 加值功能** | 收藏夾、瀏覽紀錄、圖片輪播 | F-10 ~ F-12 |

### 3.3 為什麼 Demo 不會白做

正式版會**直接沿用**現在的搜索、卡片、Modal 邏輯，只做兩件替換：

1. 「內嵌假資料」 → 「向後端要真資料」
2. 「示意假價」 → 「依帳號層級的真實底價」

換言之，**這個 Demo 驗證過的互動與規則，就是正式版的前端骨架。**
原型驗證完成後，再正式更新一版 PRD（v0.7）作為後續開發依據。

---

## 附錄：三大安全與效能承諾

| 項目 | 承諾 |
|------|------|
| **定價安全** | 底價一律由後端依帳號層級返回，前端只顯示、不過濾；API 全程驗證 Token |
| **搜索效能** | 前端運算，≤100ms 即時更新，零運算費用 |
| **現場可用性** | 觸控區 ≥48px、字體 ≥17px、高對比（戶外強光可讀）、手機優先 |
